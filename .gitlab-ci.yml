default:
  variables:
    - GIT_DEPTH=1
  image: my:latest
  tags:
    - cloud-runner

## Global stages
stages:
  - setup
  - build
  - test

setup:
  stage: setup
  script:
    - npm install
  artifacts:
    untracked: true
    expire_in: 2 hours

contract-build:
  stage: build
  dependencies:
    - setup
  script:
    - npx nx build contract

frontend-build:
  stage: build
  dependencies:
    - setup
  script:
    - npx nx build frontend

contract-test:
  stage: test
  dependencies:
    - setup
  script:
    - npx nx test contract
