default:
  tags:
    - local

## Global stages
stages:
  - setup
  - build

setup-tools:
  stage: setup
  script:
    - curl -L https://foundry.paradigm.xyz | bash
    - source /Users/lappi/.zshenv
    - foundryup
    - npm install

build-contract:
  stage: build
  script:
    - npx nx build contract
